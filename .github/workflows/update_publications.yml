name: Update Publications

on:
  schedule:
    - cron: "0 3 * * 1"   # every Monday at 03:00 UTC
  workflow_dispatch:        # also allows manual trigger from GitHub UI

permissions:
  contents: write           # needed to commit the regenerated HTML

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Regenerate publications.html
        env:
          ADS_TOKEN: ${{ secrets.ADS_TOKEN }}
        run: python generate_publications.py

      - name: Commit and push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add publications.html
          git diff --cached --quiet || git commit -m "chore: auto-update publications from NASA ADS"
          git push
